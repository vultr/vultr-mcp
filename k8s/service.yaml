apiVersion: v1
kind: Service
metadata:
  name: vultr-mcp
  labels:
    app: vultr-mcp
spec:
  selector:
    app: vultr-mcp
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
